# consul服务器的支付服务提供者端口号
server:
  port: 8006

# 服务器别名-----注册consul到注册中心名称
spring:
  application:
    name: cloud-consul-payment
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true